apiVersion: v1
kind: Service
metadata:
  name: configstore
  labels:
    run: configstore
spec:
  ports:
    - port: 80
      targetPort: 3002
      protocol: TCP
  selector:
    run: configstore
  type: ClusterIP
